services:
  mtr-runner:
    image: ghcr.io/cougz/mtr-runner:latest
    container_name: mtr-runner
    restart: unless-stopped
    cap_add:
      - NET_RAW
    env_file:
      - .env
    volumes:
      - ./data:/data/mtr
      # Note: ./data must be writable by UID 65532 (chown 65532:65532 ./data)
